# Зависимости
node_modules/

# Секреты и локальная конфигурация (ключи API, URL таблиц с приватными данными).
# Не коммитьте .env. Создайте из .env.example и храните только локально / в секретах CI.
.env
.env.local
.env.*.local

# Логи / ОС
.DS_Store
*.log
